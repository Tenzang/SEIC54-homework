<h1>
    <%= @mountain.name %>
</h1>

<%= image_tag @mountain.image, :class => "feature" if @mountain.image.present? %>

<ul>
    <li><b>Name:</b> <%= @mountain.name %></li>
    <li><b>Country:</b> <%= @mountain.country %></li>
    <li><b>Elevation:</b> <%= pluralize((number_with_delimiter @mountain.elevation), "meter") %></li>
    <li><b>Isolation:</b> <%= pluralize((number_with_delimiter @mountain.isolation), "kilometer") %></li>
</ul>

<div class="controls">
    <%= link_to "Edit mountain", edit_mountain_path(@mountain.id) %>
    | <%= link_to "Delete mountain", delete_mountain_path(@mountain.id) %>
</div>