<h2> All Movies </h2>

<% @movies.each do |movie| %>
    <p>
        <a href="/movies/<%= movie["id"] %> ">
            <%= movie["title"] %>
            </a>
        <%= "by #{ movie.director }" %>
        <img class=img src=<%= movie.image %>>
    </p>
<% end %>
